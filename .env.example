# Server Configuration
PORT=3000
NODE_ENV=production

# Data Storage
DATA_PATH=/app/data/infrastructure.json

# Application Settings
APP_TITLE=Constellation
APP_DESCRIPTION=Visual infrastructure topology dashboard

# Optional: Docker Registry (for CI/CD)
DOCKER_REGISTRY=
DOCKER_IMAGE_NAME=constellation

# Optional: Deployment Target (Tailscale)
DEPLOY_HOST=
DEPLOY_USER=
